#include<stdio.h>
#include<conio.h>
void main()
{
    int a=10;
    int b=5;
    //result is 1 (true) because 10 is greter than 5 && 5 is less than 10. so bote condition are same therefore result is 1 (true)
    printf("%d",a>b&&b<a);
}
